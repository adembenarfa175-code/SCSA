SCSA: SETTING COMPUTER SET ARCHITECTURE - PSI/O CONCEPTUAL PCB LAYOUT

This document details the major conceptual blocks of the SCSA-16 hardware, essential for the PSI/O Secure Boot mechanism, which is responsible for **Setting** the initial state of the computer.

## 1. The 16-Bit Processing Core (CPU)
... (Content remains the same, but the context is 'Setting') ...

## 4. The PSI/O Interlock (Security Hardware Concept)
This block ensures that the system is properly **Set** and Secured. The Security Check Logic performs the 7-step validation *before* allowing the Control Unit to execute the JMP instruction from the PSI/O ROM. The PSI/O is the only component trusted to **Set** the PC correctly.
